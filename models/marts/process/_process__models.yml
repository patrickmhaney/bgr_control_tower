# Generated by scripts/generate_process_views.py from seeds/process_metric_map.csv and semantic/metrics/. Do not edit by hand.
version: 2
models:
- name: mart_p2c
  description: 'GENERATED. Presentation view for P2C. Grain: one row per metric per dimension combination.
    No metrics are currently mapped to P2C; the view is generated and typed but returns no rows. An empty
    dashboard is a valid state.'
- name: mart_o2c
  description: 'GENERATED. Presentation view for O2C. Grain: one row per metric per dimension combination.
    Metrics: M1 DSO (days-to-pay proxy) (provisional), M2 Return Rate (provisional), M3 Cost Per Order
    (provisional).'
- name: mart_s2p
  description: 'GENERATED. Presentation view for S2P. Grain: one row per metric per dimension combination.
    Metrics: M1 Match Rate (provisional).'
- name: mart_f2p
  description: 'GENERATED. Presentation view for F2P. Grain: one row per metric per dimension combination.
    No metrics are currently mapped to F2P; the view is generated and typed but returns no rows. An empty
    dashboard is a valid state.'
- name: mart_i2d
  description: 'GENERATED. Presentation view for I2D. Grain: one row per metric per dimension combination.
    Metrics: M1 Inventory Accuracy (provisional), M2 On-Time Delivery (active), M3 Cost Per Shipment (active).'
- name: mart_r2p
  description: 'GENERATED. Presentation view for R2P. Grain: one row per metric per dimension combination.
    No metrics are currently mapped to R2P; the view is generated and typed but returns no rows. An empty
    dashboard is a valid state.'
- name: mart_h2r
  description: 'GENERATED. Presentation view for H2R. Grain: one row per metric per dimension combination.
    No metrics are currently mapped to H2R; the view is generated and typed but returns no rows. An empty
    dashboard is a valid state.'
- name: mart_i2s
  description: 'GENERATED. Presentation view for I2S. Grain: one row per metric per dimension combination.
    No metrics are currently mapped to I2S; the view is generated and typed but returns no rows. An empty
    dashboard is a valid state.'
- name: mart_m2o
  description: 'GENERATED. Presentation view for M2O. Grain: one row per metric per dimension combination.
    No metrics are currently mapped to M2O; the view is generated and typed but returns no rows. An empty
    dashboard is a valid state.'
